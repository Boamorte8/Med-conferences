import template from './conferences.html'
import controller from './conferences.controller'

let conferencesComponent = {
    templateUrl: template,
    controller: controller,
    controllerAs: 'vm'
}

export default conferencesComponent